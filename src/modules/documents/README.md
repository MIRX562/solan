# Documents Module

Owns document metadata, immutable versions, attachments, storage adapters, and rendering adapters.

Invoice rendering consumes a shared invoice view model and produces PDF and JPG outputs without owning invoice lifecycle rules.
