# Auth Infrastructure

Owns Better Auth configuration, session access helpers, and authorization context.

Business modules consume session/user contracts from this boundary instead of importing Better Auth internals.
