# Shared Components

Owns reusable presentation components and application-level UI primitives.

Domain-specific components should remain inside their owning module so the domain UI and workflows evolve together.
